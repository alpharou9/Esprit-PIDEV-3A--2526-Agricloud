{% extends 'base.html.twig' %}

{% block title %}New Order - AgriCloud{% endblock %}

{% block body %}
<div class="mb-4">
    <h3 class="fw-bold mb-1">Create order</h3>
    <p class="text-muted mb-0">Create an order row that matches the `orders` table from your SQL dump.</p>
</div>

<div class="card border-0 shadow-sm">
    <div class="card-body">
        {% include 'order/_form.html.twig' with {form: form, button_label: 'Create order'} %}
    </div>
</div>
{% endblock %}
