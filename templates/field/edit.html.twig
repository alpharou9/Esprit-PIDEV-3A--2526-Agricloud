{% extends 'base.html.twig' %}

{% block title %}Edit Field{% endblock %}

{% block body %}
    <h1>Edit Field</h1>

    {{ include('field/_form.html.twig', {'button_label': 'Update'}) }}

<a href="{{ path('app_field_index', {'farm_id': field.farmid.id}) }}" class="btn btn-secondary mt-3">back to list</a>

    {{ include('field/_delete_form.html.twig') }}
{% endblock %}
