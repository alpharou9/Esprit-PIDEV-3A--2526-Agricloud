<form method="post" action="{{ path('app_farm_delete', {'id': farm.id}) }}" onsubmit="return confirm('Are you sure?');" style="display:inline-block;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ farm.id) }}">
    <button class="btn btn-outline-danger btn-sm">Delete</button>
</form>